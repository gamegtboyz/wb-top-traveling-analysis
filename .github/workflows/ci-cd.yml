name: CI/CD Piepeline

on:
    push:
        branches: ["main"]
    pull_request:

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            
            - uses: actions/setup-python@v5
              with:
                  python-version: '3.11'

            - name: Install dependencies
              run: |
                pip install --upgrade pip
                pip install .[dev]

            - name: Run tests
              run: pytest

    build-and-deploy:
        runs-on: ubuntu-latest
        needs: test # only run if tests pass
        if: github.ref == 'refs/heads/main'

        steps:
            - uses: actions/checkout@v4

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v4
              with:
                aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                aws-region: ${{ secrets.AWS_REGION }}

            - name: login to Amazon ECR
              id: ecr
              uses: aws-actions/amazon-ecr-login@v2

            - name: Build, tag, and push image to Amazon ECR
              run: |
                IMAGE_URI=${{ secrets.AWS_ECR_REPO }}:latest
                docker build -t $IMAGE_URI -f Dockerfile.dashboard .
                docker push $IMAGE_URI

            - name: Deploy to App Runner
              run: |
                aws apprunner update-service \
                  --service-arn arn:aws:apprunner:${{ secrets.AWS_REGION }}:783972422656:service/wb-top-traveling-analysis/67eaf217061049f5bb140100fa988c2d \
                  --source-configuration "ImageRepository={ImageIdentifier=${{ secrets.AWS_ECR_REPO }}:latest,ImageRepositoryType=ECR}"

                echo "Waiting for App Runner deployment to complete..."
                aws apprunner update-service \
                  --service-arn arn:aws:apprunner:${{ secrets.AWS_REGION }}:783972422656:service/wb-top-traveling-analysis/67eaf217061049f5bb140100fa988c2d